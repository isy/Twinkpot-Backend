<% icon_sizes = Rails.configuration.serviceworker.icon_sizes %>
{
  "name": "Twinkpot - フォトジェニックな写真から行きたいところを",
  "short_name": "Twinkpot",
  "start_url": "/home",
  "icons": [
  <% icon_sizes.map { |s| "#{s}x#{s}" }.each.with_index do |dim, i| %>
    {
      "src": "<%= asset_path "serviceworker-rails/logo-#{dim}.png" %>",
      "sizes": "<%= dim %>",
      "type": "image/png"
    }<%= i == (icon_sizes.length - 1) ? '' : ',' %>
  <% end %>
  ],
  "theme_color": "#fdd934",
  "background_color": "#FFFFFF",
  "display": "standalone",
  "orientation": "portrait"
}
